import { useData } from "../../context/DataContext";
import SectionContainer from "../smallComponents/SectionContainer";

const Contact = () => {
 return (
    <SectionContainer bg="bg-yellow-500" >
        <div id="contact" className="scroll-mt-[394px] text-textColor">
            <h1 className="">Contact Section</h1>
        </div>
    </SectionContainer>
  )
}

export default Contact